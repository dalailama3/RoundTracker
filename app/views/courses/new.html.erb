<div class="container">
  <div class="page-header">
     <h1>Add Course</h1>
  </div>
</div>

<% @course.errors.full_messages.each do |msg| %>
  <ul>
    <li><%= msg %></li>
  </ul>
<% end %>

<%= render "form", course: @course, action: :new %>
