<h1>Welcome <%= current_user.email %></h1>
<h1>My rounds</h1>

<%= link_to "Add Round", new_round_url %>


<ul>
  <% @rounds.each do |round| %>
    <li><%= link_to "#{round.course_name} round", round_url(round) %></li>
  <% end %>

</ul>
